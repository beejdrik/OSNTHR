#include <iostream>
#include "Graph.h"
using namespace std;

int main() {
    std::cout << "Hello, World!" << std::endl;

    Graph g;

    //g.printMap();

    /*
    g.addEdge(7, 1, 2);
    g.addEdge(7, 6, 3);
    g.addEdge(7, 8, 1);
    g.addEdge(7, 3, 4);
    g.printEdges(0);
    */

    return 0;
}
